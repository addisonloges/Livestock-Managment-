import {groupMembers} from './management-groups.ts';
import type {FarmEvent} from './farm-events.ts';
import {type Animal,label} from './livestock.ts';
import {statusAt} from './animal-status.ts';
export type ReportFilter={groupId?:string;groupDate?:string;query:string;status:string;sex:string;breed:string;birthYear:string;sort:'Animal'|'Breed'|'Birth year'};
export const emptyReportFilter:ReportFilter={query:'',status:'All',sex:'All',breed:'',birthYear:'',sort:'Animal'};
export function validateReportFilter(f:ReportFilter){if(f?.groupId!==undefined&&(typeof f.groupId!=='string'||f.groupId.length>100)||f?.groupDate!==undefined&&(typeof f.groupDate!=='string'||f.groupDate!==''&&!/^\d{4}-\d{2}-\d{2}$/.test(f.groupDate)))throw Error('Invalid group report filter.');if(!f||typeof f.query!=='string'||f.query.length>200||!['All','Active','Sold','Dead','Culled','Transferred'].includes(f.status)||!['All','Unknown','Female','Male','Castrated male'].includes(f.sex)||typeof f.breed!=='string'||f.breed.length>200||typeof f.birthYear!=='string'||f.birthYear!==''&&!/^\d{4}$/.test(f.birthYear)||!['Animal','Breed','Birth year'].includes(f.sort))throw Error('Invalid report filters.');}
export function filterReportAnimals(animals:Animal[],f:ReportFilter,date:string,events:FarmEvent[]=[]){const members=f.groupId?groupMembers(events,f.groupId,f.groupDate||date):null;return animals.filter(a=>(!members||members.has(a.id))&&(f.status==='All'||statusAt(a,date)===f.status)&&(f.sex==='All'||a.sex===f.sex)&&(!f.breed||a.breed.toLowerCase().includes(f.breed.toLowerCase()))&&(!f.birthYear||String(a.birthYear||a.dob?.slice(0,4)||'')===f.birthYear)&&[label(a),a.breed].some(v=>v.toLowerCase().includes(f.query.toLowerCase()))).sort((a,b)=>f.sort==='Birth year'?(a.birthYear||9999)-(b.birthYear||9999)||label(a).localeCompare(label(b)):f.sort==='Breed'?a.breed.localeCompare(b.breed)||label(a).localeCompare(label(b)):label(a).localeCompare(label(b),undefined,{numeric:true}));}
