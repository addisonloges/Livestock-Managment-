import assert from 'node:assert/strict';import {mapImportRows,matchImportField,swapImportTags} from '../lib/animal-import.ts';
const grid=[['Tag 1','Tag 2','Tag Color'],['01','02','White/Yellow'],['03','04','/Blue'],['05','','Green/']];const rows=mapImportRows(grid,grid[0].map(matchImportField),'Sheep','2026');assert.equal(rows[0].rightTagColor,'White');assert.equal(rows[0].leftTagColor,'Yellow');assert.equal(rows[1].rightTagColor,'');assert.equal(rows[1].leftTagColor,'Blue');const s=swapImportTags(rows[0]);assert.equal(s.rightTag,'02');assert.equal(s.rightTagColor,'Yellow');assert.deepEqual(swapImportTags(s),rows[0]);console.log('PASS: slash colors, empty first color, tag/color swap and swap reversal');
