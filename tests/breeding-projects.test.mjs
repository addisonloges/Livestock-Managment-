import test from 'node:test';import assert from 'node:assert/strict';import {shares,offspring,targetDistance,validateProject,addDays} from '../lib/breeding-projects.ts';import {validateGroup} from '../lib/breeding.ts';
test('offspring composition requires complete parental coverage',()=>{const a={pedigreeInfo:JSON.stringify({breedComposition:[{breed:'Dorper',percent:100}]})},b={pedigreeInfo:'{}'};assert.deepEqual(offspring(a,b),[]);assert.equal(targetDistance(offspring(a,b),[{breed:'Dorper',percent:100}]),null);assert.equal(targetDistance(offspring(a,a),[{breed:'Dorper',percent:100}]),0);assert.throws(()=>shares([{breed:'a',percent:70},{breed:'b',percent:40}]));assert.throws(()=>shares([{breed:'a',percent:50},{breed:'A',percent:50}]));});
test('project targets total 100 and timing can remain unspecified',()=>{const p={name:'Composite',targets:[{breed:'A',percent:100}],gestationMin:null,gestationMax:null,flushingDays:null,crayonDays:null};assert.doesNotThrow(()=>validateProject(p));assert.throws(()=>validateProject({...p,gestationMin:150}));assert.throws(()=>validateProject({...p,targets:[{breed:'A',percent:25}]}));assert.equal(addDays('2026-12-31',1),'2027-01-01')});
test('pregnancy results validate member, date and optional fetus count',()=>{const animals=[{id:'r',sex:'Male',species:'Sheep',firstYear:2026},{id:'e',sex:'Female',species:'Sheep',firstYear:2026}],c={id:crypto.randomUUID(),eweId:'e',date:'2026-09-05',result:'Pregnant',method:'Ultrasound',fetuses:null,notes:''},g={name:'G',ramId:'r',eweIds:['e'],year:2026,species:'Sheep',state:'Exposed',start:'2026-09-01',end:'',notes:'',pregnancyChecks:[c]};assert.doesNotThrow(()=>validateGroup(g,animals));assert.throws(()=>validateGroup({...g,pregnancyChecks:[{...c,eweId:'missing'}]},animals));assert.throws(()=>validateGroup({...g,pregnancyChecks:[{...c,date:'2026-08-01'}]},animals));assert.throws(()=>validateGroup({...g,pregnancyChecks:[{...c,result:'Open',fetuses:2}]},animals));});
