import {test} from 'node:test';import assert from 'node:assert/strict';
import {normalizeOwners,resolvedOwnership,ownershipAt} from '../lib/ownership.ts';import {fileType} from '../lib/animal-files.ts';
test('ownership exact shares and names',()=>{assert.deepEqual(normalizeOwners([{name:'A',percent:'33.33'},{name:'B',percent:'66.67'}],false),[{name:'A',basisPoints:3333},{name:'B',basisPoints:6667}]);assert.throws(()=>normalizeOwners([{name:'A',percent:'99.99'}],false));assert.throws(()=>normalizeOwners([{name:'A',percent:'50'},{name:'a',percent:'50'}],false));assert.throws(()=>normalizeOwners([{name:'A',percent:'100.001'}],false));assert.deepEqual(normalizeOwners([],true),[])});
test('ownership historical and correction resolution',()=>{const events=[{operationId:'a',date:'2025-01-01',owners:[{name:'A',basisPoints:10000}]},{operationId:'b',date:'2026-01-01',owners:[{name:'B',basisPoints:10000}]},{operationId:'c',date:'2026-02-01',owners:[{name:'C',basisPoints:10000}],supersedes:'b'}];assert.equal(resolvedOwnership(events).length,2);assert.equal(ownershipAt(events,'2024-12-31'),undefined);assert.equal(ownershipAt(events,'2026-01-15').owners[0].name,'A');assert.equal(ownershipAt(events,'2026-03-01').owners[0].name,'C')});
test('uploads only recognize allowed file signatures',()=>{assert.equal(fileType(new Uint8Array([137,80,78,71,13,10,26,10])),'image/png');assert.equal(fileType(new TextEncoder().encode('%PDF-1.4')),'application/pdf');assert.equal(fileType(new TextEncoder().encode('<script>alert(1)</script>')),'');assert.equal(fileType(new Uint8Array([255,216,255])),'image/jpeg')});
