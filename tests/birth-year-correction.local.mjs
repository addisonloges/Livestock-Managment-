import assert from 'node:assert/strict';
const root='http://127.0.0.1:5173/api/flock',epoch=(await fetch(root)).headers.get('X-Flock-Epoch');async function post(body){const r=await fetch(root,{method:'POST',headers:{'Content-Type':'application/json','X-Flock-Epoch':epoch},body:JSON.stringify(body)});const data=await r.json();assert.equal(r.status,200,JSON.stringify(data));return data}const id=crypto.randomUUID();await post({action:'animal',year:'2026',data:{id,species:'Sheep',name:'Birth-year reservation QA',sex:'Unknown',origin:'Purchased',birthYear:2026,firstYear:2026,dob:'',breed:'',rightTag:'',leftTag:'',eid:'',sire:'',dam:''}});const read=async()=>(await(await fetch(root)).json()).animals.find(a=>a.id===id);let a=await read();const original=a.birthSequence;for(const birthYear of [2025,2026]){await post({action:'edit',year:'2026',data:{id,version:a.version,name:a.name,breed:a.breed,dob:'',birthYear,operationId:crypto.randomUUID(),reason:'Test birth-year correction'}});a=await read();assert.equal(a.birthYear,birthYear)}assert.ok(a.birthSequence>original);assert.equal(a.version,3);console.log('PASS birth-year corrections report success and never reuse the prior year number');
