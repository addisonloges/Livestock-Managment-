import {env} from 'cloudflare:workers';
import {GET as fullBackup} from '../full/route';
import {GET as recordsBackup} from '../route';
import {isUuid} from '@/lib/lambing';
export const dynamic='force-dynamic';
const json=(v:unknown,status=200)=>Response.json(v,{status,headers:{'Cache-Control':'no-store'}});
export async function GET(req:Request){try{if(!env.BUCKET)throw Error('Backup storage unavailable.');const key=new URL(req.url).searchParams.get('key');if(key){if(!/^backup-vault\/manual\/[0-9a-f-]{36}\.(json|tar)$/.test(key))throw Error('Invalid backup reference.');const object=await env.BUCKET.get(key);if(!object)return json({error:'Backup not found.'},404);return new Response(object.body,{headers:{'Cache-Control':'no-store','Content-Type':key.endsWith('.tar')?'application/x-tar':'application/json','Content-Disposition':'attachment; filename="clarksons-'+key.split('/').at(-1)+'"'}})}const cursor=new URL(req.url).searchParams.get('cursor')||undefined;const list=await env.BUCKET.list({prefix:'backup-vault/manual/',limit:100,cursor});return json({backups:list.objects.map(o=>({key:o.key,date:o.uploaded,size:o.size,full:o.key.endsWith('.tar')})),cursor:list.truncated?list.cursor:null});}catch(e){return json({error:(e as Error).message},503)}}
export async function POST(req:Request){try{const b:any=await req.json();if(!isUuid(b.operationId)||!['full','records'].includes(b.kind))throw Error('Choose a records or full backup.');if(!env.BUCKET)throw Error('Backup storage unavailable.');const key='backup-vault/manual/'+b.operationId+(b.kind==='full'?'.tar':'.json');if(await env.BUCKET.head(key))return json({saved:true,key});const response=await(b.kind==='full'?fullBackup():recordsBackup());if(!response.ok)throw Error('Backup could not be created. '+await response.text());if(b.kind==='full'){const length=Number(response.headers.get('Content-Length'));if(!Number.isSafeInteger(length)||length<=0||!response.body)throw Error('Full backup length unavailable.');const stream=new FixedLengthStream(length);await Promise.all([response.body.pipeTo(stream.writable),env.BUCKET.put(key,stream.readable,{onlyIf:{etagDoesNotMatch:'*'},httpMetadata:{contentType:'application/x-tar'}})]);}else{await env.BUCKET.put(key,await response.text(),{onlyIf:{etagDoesNotMatch:'*'},httpMetadata:{contentType:'application/json'}});}return json({saved:true,key});}catch(e){return json({error:(e as Error).message},503)}}
