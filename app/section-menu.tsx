"use client";
import {workspaces} from './workspace-sidebar';
export default function SectionMenu({value,onChange,species,deletedCount}:{value:string;onChange:(v:string)=>void;species:string;deletedCount:number}){const group=workspaces.find(g=>g.items.some(i=>i.id===value));if(!group)return null;return <nav className="workbook-tabs" aria-label={`${group.name} tools`}>{group.items.map(i=><button key={i.id} type="button" aria-current={i.id===value?'page':undefined} onClick={()=>onChange(i.id)}>{i.id==='lambing'?(species==='Sheep'?'Lambing':'Kidding'):i.name}{i.id==='deleted'&&<span className="workbook-count">{deletedCount}</span>}</button>)}</nav>}
